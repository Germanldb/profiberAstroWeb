---
import Image from "astro/components/Image.astro";
import imgBody from "../assets/boy.png";
import imgGirl from "../assets/girl.png";
import people from "../assets/main_people.png";
import location_plus from "../assets/icons/location_plus.svg";
import wire_icon from "../assets/icons/wire.svg";
import vector_wave from "../assets/background/vector_wave.svg";

// src/components/Hero.astro
---

<section class="relative text-white text-center pt-20 overflow-hidden">
  <!-- <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-b from-[#4D00B0] to-[#6f00ff] z-0"></div> -->

  <!-- <div class="absolute -top-1/4 -left-1/4 w-1/2 h-1/2 bg-yellow-400 rounded-full filter blur-3xl opacity-30 animate-blob"></div> -->
  <!-- <div class="absolute -bottom-1/4 -right-1/4 w-1/2 h-1/2 bg-pink-500 rounded-full filter blur-3xl opacity-30 animate-blob animation-delay-2000"></div> -->
<div>
  <Image src={vector_wave} alt="Vector Wave" class="absolute inset-0 w-full h-full object-cover z-[-2]" />
</div>
  <div class="relative z-10 w-full max-w-7xl mx-auto px-4">
    <div class="absolute left-10 top-1/2 -translate-y-1/2 hidden lg:block">
      <!-- <Image src={imgBody} alt="Man on phone" class="rounded-lg" /> -->
    </div>

    <div class="max-w-2xl mx-auto">
      <h1 class="text-5xl md:text-6xl font-bold mb-4">La Solución Total En Conectividad</h1>
      <h1
        class="text-3xl md:text-6xl font-bold mb-4 inline-block px-3 md:px-6 py-1 pb-[20px] border-[#FD3CFC] border-4 rounded-xl"
      >
        ¡Sin Limites!
      </h1>
    </div>

    <div class="relative w-full flex justify-center mt-2">
      <!-- wrapper: column on small screens, switches to row behavior on md+; absolute panels active only on md+ -->
      <div class="relative w-full flex flex-col items-center justify-center md:flex-row">

        <!-- Left info panel: becomes absolute on md+; on small screens it's above the image -->
        <div class="md:absolute md:left-[-80px] md:top-[-80px] order-1 md:order-none flex flex-col items-center text-center max-w-[420px] p-4 w-full md:w-auto">
          <div class="bg-[#8F4DEF] rounded-full p-4 md:p-6 mb-2 h-24 w-24 md:h-28 md:w-28 flex items-center justify-center">
            <Image src={location_plus} alt="cobertura" class="rounded-lg mb-0 w-16 h-auto z-1" />
          </div>
          <h2 class="font-semibold">Cobertura en Crecimiento</h2>
          <p class="text-sm">
            La construcción nunca es un obstáculo para llevarte internet de alta velocidad,
            televisión y telefonía confiable.
          </p>
        </div>

        <!-- Center image -->
        <div class="z-10 order-3 md:order-none pt-14">
          <Image
            src={people}
            alt="people on phone"
            class="rounded-lg shadow-xl  md:w-[420px] h-auto object-cover"
          />
        </div>

        <!-- Right info panel: becomes absolute on md+; on small screens it's below the image -->
        <div class="md:absolute md:right-[-100px] md:top-[-80px] order-2 md:order-none flex flex-col items-center text-center max-w-[420px] p-4 w-full md:w-auto">
          <div class="bg-[#8F4DEF] rounded-full p-4 md:p-6 mb-2 h-24 w-24 md:h-28 md:w-28 flex items-center justify-center">
            <Image src={wire_icon} alt="innovacion" class="rounded-lg mb-0 h-16 w-auto" />
          </div>
          <h2 class="font-semibold">Constante Innovación</h2>
          <p class="text-sm">
            Infraestructura avanzada con fibra óptica, tecnología 5G y servicios por antena,
            garantizamos una conexión estable, rápida y sin interrupciones.
          </p>
        </div>

      </div>
    </div>

    <div class="mb-12 flex justify-center px-4">
      <p class="py-6 px-4 text-xl border-4 border-[#FD3CFC] rounded-xl w-full max-w-[820px] text-justify">
        <span class="font-bold"> Conectamos hogares,</span> negocios y todo tipo de espacios en cualquier
        parte de Puerto Rico. Ya sea en la ciudad, en la montaña o <span class="font-bold"
          >en zonas de difícil acceso.</span
        >
      </p>
    </div>

    <!-- <div class="absolute right-10 top-1/2 -translate-y-1/2 hidden lg:block">
      <Image src={imgGirl} alt="Woman on phone" class="rounded-lg" />
    </div> -->
  </div>
</section>

<style>

  section{
    background-image: url('../assets/background/network_back.svg');
  }
  .animate-blob {
    animation: blob 7s infinite;
  }
  .animation-delay-2000 {
    animation-delay: 2s;
  }
  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }
</style>
